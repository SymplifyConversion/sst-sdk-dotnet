image: mcr.microsoft.com/dotnet/sdk:5.0

stages:
  - build
  - test

variables:
  project: "sst-sdk-dotnet"

before_script:
  - "dotnet restore"

build:
  stage: build
  script:
    - "dotnet build"

test:
  stage: test
  script:
    - "dotnet test"